name: retina-renal-ai
channels: [conda-forge, pytorch, nvidia]
dependencies:
  - python=3.10
  - pytorch::pytorch>=2.2
  - pytorch::torchvision
  - pytorch::cpuonly
  - pip
  - pip:
      - timm>=0.9.2
      - pandas>=2.2
      - scikit-learn>=1.4
      - numpy>=1.26
      - albumentations>=1.4
      - opencv-python-headless>=4.9
      - rich>=13.7
      - pyyaml>=6.0
      - tqdm>=4.66
